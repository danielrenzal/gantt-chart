import React from 'react';
import ReactDOM from 'react-dom/client';
import StackSchedule from './stack_schedule';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  // <React.StrictMode>
    <StackSchedule />
  // </React.StrictMode>
);
